<?xml version="1.0" encoding="utf-8"?>
<mx:VBox xmlns:fx="http://ns.adobe.com/mxml/2009" 
		 xmlns:s="library://ns.adobe.com/flex/spark" 
		 xmlns:mx="library://ns.adobe.com/flex/mx"
		 verticalScrollPolicy="off"
		 horizontalScrollPolicy="off"
		 creationComplete="creationComplete()">
	<fx:Script>
		<![CDATA[
			import mx.controls.Alert;
			import mx.controls.List;
			import mx.core.FlexGlobals;
			import mx.managers.PopUpManager;
			public var xml:XML;
			public function creationComplete():void
			{
				
			}
			private function addPopup():void
			{
				var list:List = new List();
				list.dataProvider = [{label:"aaa"},{label:"bbb"}];
				PopUpManager.addPopUp(list,DisplayObject(FlexGlobals.topLevelApplication));
				PopUpManager.centerPopUp(list);
			}
		]]>
	</fx:Script>
	<fx:Declarations>
		<!-- Place non-visual elements (e.g., services, value objects) here -->
	</fx:Declarations>
	<mx:Image source="@Embed(source='assets/down.png')" buttonMode="true" click="addPopup()"/>
</mx:VBox>
